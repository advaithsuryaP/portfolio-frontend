export const TIMELINE_CONSTANTS = {
    /** Minimum height of each month tick row. Rows grow when multiple moments stack. */
    rowHeight: 16,
    /** Vertical gap between month rows. */
    rowGap: '0.25rem'
} as const;
